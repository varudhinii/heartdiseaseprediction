# HeartDiseasePrediction
A machine learning–based heart disease prediction system built using clinical patient data. The project involves data preprocessing, feature scaling, model training using Logistic Regression and Random Forest, and deployment through a Streamlit web app for real-time prediction
